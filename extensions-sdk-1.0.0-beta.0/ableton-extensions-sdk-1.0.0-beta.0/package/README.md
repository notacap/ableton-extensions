# @ableton-extensions/sdk

TypeScript SDK for building extensions for Ableton Live. Provides typed access
to Live's data model — song, tracks, clips, devices, and more — along with
APIs for commands, UI (context menus), and file system access.
